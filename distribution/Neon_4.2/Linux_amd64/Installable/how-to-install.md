Neon is distributed as a `.lpk` (Linux Package) file. To install Neon, make the file `Neon_installer.lpk` executable, and then run it.
To learn more about the LPK format, see https://linux-package-maker.raphaael.fr/
